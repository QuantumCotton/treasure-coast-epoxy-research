# 2025-01-27

## Agent Capabilities & Strategy

**Important:** I can spawn sub-agents using `sessions_spawn` to:
- Delegate complex tasks
- Save my memory tokens
- Parallelize work
- Use different models if needed

**Available agents:**
- `main` - Currently the only available agent (allowAny: false)
- Can use model: `zai/glm-4.7` (very smart, recommended)

**When to spawn agents:**
- Multi-step research tasks
- Complex installations/setup
- Independent work streams
- When user explicitly requests it

**Commands:**
```bash
# List available agents
agents_list

# Spawn an agent with a task
sessions_spawn(task="...", agentId="main", model="zai/glm-4.7")
```

**Note:** User requested using glm-4.7 for smart sub-agents when needed to save tokens.
